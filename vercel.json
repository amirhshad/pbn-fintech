{
  "buildCommand": "cd pbn-fintech-app/web && npm install && npm run build",
  "outputDirectory": "pbn-fintech-app/web/build",
  "installCommand": "cd pbn-fintech-app/web && npm install",
  "framework": null,
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
